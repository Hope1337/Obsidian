**Regex** (viết tắt của **Regular Expression**) là **biểu thức chính quy**, dùng để **mô tả mẫu (pattern)** trong chuỗi văn bản.

Nó giúp:

- Tìm kiếm (search)

- So khớp (match)

- Thay thế (replace)

- Trích xuất (extract)